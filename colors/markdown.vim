" Markdown is peculiar about tabs vs spaces. Four spaces is a <pre> block, so
" just don't use tabs and set the indent size to four spaces.
set expandtab
set ts=4
set sw=4
let b:strip_trailing_ws = 0
